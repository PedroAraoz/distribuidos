apiVersion: v1
kind: Service
metadata:
  name: {{ include "dist.fullname" . }}
  labels:
    {{- include "dist.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dist.selectorLabels" . | nindent 4 }}
