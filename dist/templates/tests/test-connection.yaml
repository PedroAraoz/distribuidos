apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "dist.fullname" . }}-test-connection"
  labels:
    {{- include "dist.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "dist.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
